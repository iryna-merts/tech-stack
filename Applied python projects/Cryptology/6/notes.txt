протокола Дифі-Хеллмана для обміну ключами

узгодження ключа, що дозволяє двом учасникам, 
що не мають жодних попередніх даних один про одного, 
отримати спільний секретний ключ 
із використанням незахищеного каналу зв'язку.

Аліса і Боб таємно обирають два випадкових цілих числа sA та sB, 
	в інтервалі [0, |G| ? 1]. 
Потім вони таємно обчислють числа aA = gsA та aB = gsB відповідно, 
та обмінюються ними через незахищений канал передачі даних. 
Аліса та Боб обчислюють aBA = aBsA = gsBsA та aAB = aAsB = gsAsB відповідно. 
aAB = aBA, і тому це число може служити спільним таємним ключем K Аліси та Боба. 

Швидкодія та криптографічна стійкість залежать від обраного простору 
(циклічної групи)
Зокрема: мультиплікативні групи над великими скінченними полями 
(поля простих чисел або розширення), мультиплікативна група залишків за модулем складеного
 числа, еліптичні криві над скінченними полями, якобіан гіпереліптичних кривих над 
скінченним полем, та факторгрупи уявних квадратичних полів. 

криптосистем із відкритим ключем

використовується для забезпечення цілковитої прямої секретності 
в недовговічних режимах Transport Layer Security
______________________________________________________________________
базовий варіантпротоколу узгодження ключа анонімний, 
тут відсутня можливість автентифікації абонентів. 
Таким чином, протокол вразливий для атаки «людина посередині».

Припустімо, що зловмисник C здатен здійснювати підміну повідомлень, 
якими обмінюються Аліса та Боб. Тоді він може згенерувати числа s*A та s*B,
 і відповідно, отримати два узгоджених ключа: gsAs*B та gs*AsB. 

Обчислити функцію Діффі-Геллмана можна через обчислення a з A=g^a(mod p) і B=g^{b}(mod p). 
Це є проблема дискретного логарифму, яка обчислювальна нерозв'язна при достатньо великих p
Обчислення дискретного алгоритму за модулем p потребує приблизно стільки ж часу 
як факторизація добутку двох простих чисел розміру як і p, 
саме на це покладається безпека криптосистем RSA. 
Отже протокол Діффі-Геллмана приблизно так само безпечний як безпечний RSA